---
title: 17 技巧
article: false
category:
  - 代码笔记
  - LC Hot100
  - 技巧
---


## [<font style="color:#DF2A3F;">136. 只出现一次的数字</font>](https://leetcode.cn/problems/single-number/)[简单]
给你一个 **非空** 整数数组 `nums` ，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。

你必须设计并实现线性时间复杂度的算法来解决此问题，且该算法只使用常量额外空间。

<!-- 这是一张图片，ocr 内容为： -->
![](https://jiwang.cc.cd/PicGo/lchot100/2026-1774278546-d3182a.png)

**思路：**使用异或运算，异或运算有以下三个性质。

+ 任何数和 0 做异或运算，结果仍然是原来的数，即 a^0=a。
+ 任何数和其自身做异或运算，结果是 0，即 a^a=0。

```java
class Solution {
    public int singleNumber(int[] nums) {
        int ans = 0;
        for (int num : nums) {
            ans = num ^ ans;
        }
        return ans;
    }
}
```

## [<font style="color:#DF2A3F;">169. 多数元素</font>](https://leetcode.cn/problems/majority-element/)[简单]
给定一个大小为 `n`的数组 `nums` ，返回其中的多数元素。多数元素是指在数组中出现次数 **大于**`⌊ n/2 ⌋` 的元素。你可以假设数组是非空的，并且给定的数组总是存在多数元素。

<!-- 这是一张图片，ocr 内容为： -->
![](https://jiwang.cc.cd/PicGo/lchot100/2026-1774278546-4d2bef.png)

**思路：**<font style="color:rgb(15, 17, 21);">Boyer-Moore(摩尔)投票算法，这个算法的思想很巧妙：</font>**<font style="color:rgb(15, 17, 21);">"同归于尽"法</font>**

<font style="color:rgb(15, 17, 21);">想象一下：</font>

+ <font style="color:rgb(15, 17, 21);">把数组看作不同阵营的士兵在打仗</font>
+ <font style="color:rgb(15, 17, 21);">每个士兵遇到同阵营的就结盟，遇到不同阵营的就同归于尽</font>
+ <font style="color:rgb(15, 17, 21);">最后活下来的肯定是人数最多的那个阵营</font>

需要定义两个变量，res 用来记录多数元素， count 用于记录元素的数量

count 并不是真实的数量，它会随着遍历数组不断的变化。

假设第一个数为多数元素，遍历数组时，每遇到一个和多数元素相同的数则计数count+1，否则count-1。

如果发现计数count为0，则更换多数元素

```java
class Solution {
    public int majorityElement(int[] nums) {
        int count = 0;
        int res = 0;
        for(int num : nums){
            if(count == 0){
                res = num;
            }
            if(num == res){
                count++;
            }else{
                count--;
            }
        }
        return res;
    }
}
```

## [<font style="color:#DF2A3F;">75. 颜色分类</font>](https://leetcode.cn/problems/sort-colors/)[中等]
给定一个包含红色、白色和蓝色、共 `n`个元素的数组 `nums` ，[**原地**](https://baike.baidu.com/item/%E5%8E%9F%E5%9C%B0%E7%AE%97%E6%B3%95)对它们进行排序，使得相同颜色的元素相邻，并按照红色、白色、蓝色顺序排列。

我们使用整数 `0`、 `1` 和 `2` 分别表示红色、白色和蓝色。

必须在不使用库内置的 sort 函数的情况下解决这个问题。

<!-- 这是一张图片，ocr 内容为： -->
![](https://jiwang.cc.cd/PicGo/lchot100/2026-1774278546-f98251.png)

**思路：**

一次遍历计算出 0 和 1 的个数，剩下的都是 2

```java
class Solution {
    public void sortColors(int[] nums) {
        int n = nums.length;
        int red = 0;
        int white = 0;
        for (int i = 0; i < n; i++) {
            if (nums[i] == 0) {
                red++;
            } else if (nums[i] == 1) {
                white++;
            }
        }
        int cur = 0;
        while (red > 0) {
            nums[cur] = 0;
            red--;
            cur++;
        }
        while (white > 0) {
            nums[cur] = 1;
            white--;
            cur++;
        }
        while (cur < n) {
            nums[cur] = 2;
            cur++;
        }
    }
}
```

## [<font style="color:#DF2A3F;">31. 下一个排列</font>](https://leetcode.cn/problems/next-permutation/)[中等]
整数数组的一个 **排列** 就是将其所有成员以序列或线性顺序排列。

+ 例如，`arr = [1,2,3]` ，以下这些都可以视作 `arr` 的排列：`[1,2,3]`、`[1,3,2]`、`[3,1,2]`、`[2,3,1]` 。

整数数组的 **下一个排列** 是指其整数的下一个字典序更大的排列。更正式地，如果数组的所有排列根据其字典顺序从小到大排列在一个容器中，那么数组的 **下一个排列** 就是在这个有序容器中排在它后面的那个排列。如果不存在下一个更大的排列，那么这个数组必须重排为字典序最小的排列（即，其元素按升序排列）。

+ 例如，`arr = [1,2,3]` 的下一个排列是 `[1,3,2]` 。
+ 类似地，`arr = [2,3,1]` 的下一个排列是 `[3,1,2]` 。
+ 而 `arr = [3,2,1]` 的下一个排列是 `[1,2,3]` ，因为 `[3,2,1]` 不存在一个字典序更大的排列。

给你一个整数数组 `nums` ，找出 `nums` 的下一个排列。必须[**原地**](https://baike.baidu.com/item/%E5%8E%9F%E5%9C%B0%E7%AE%97%E6%B3%95)修改，只允许使用额外常数空间。

<!-- 这是一张图片，ocr 内容为： -->
![](https://jiwang.cc.cd/PicGo/lchot100/2026-1774278546-ebf7cd.png)

**思路：**

<font style="color:rgb(15, 17, 21);">从右向左找到第一个升序对（即 </font>`<font style="color:rgb(15, 17, 21);background-color:rgb(235, 238, 242);">nums[i-1] < nums[i]</font>`<font style="color:rgb(15, 17, 21);"> 的位置），将左边的较小数 </font>`<font style="color:rgb(15, 17, 21);background-color:rgb(235, 238, 242);">nums[i-1]</font>`<font style="color:rgb(15, 17, 21);"> 与右边第一个大于它的数交换，然后将 </font>`<font style="color:rgb(15, 17, 21);background-color:rgb(235, 238, 242);">i</font>`<font style="color:rgb(15, 17, 21);"> 右侧原本降序的部分反转成升序，就能得到字典序中刚好比当前排列大的下一个排列。</font>

```java
class Solution {
    public void nextPermutation(int[] nums) {
        int n = nums.length;
        
        // 1. 从后往前找到第一个升序对的位置 i-1
        // 即找到第一个 nums[i-1] < nums[i] 的位置

        //按照直觉来说,找的应该是 最靠近末尾 的 顺序对 ,只要把这个顺序颠倒了,就能找到目标
        int i = n - 1;
        while (i >= 1 && nums[i - 1] >= nums[i]) {
            i--;
        }
        // 这时候i指向的是右边的那个数
        
        // 2. 如果找到了这样的位置（i >= 1）
        if (i >= 1) {
            // i-1 就是我们要找的"下降点"  yes
            int pivot = i - 1;
            
            // 3. 从后往前找到第一个大于 nums[pivot] 的数
            int j = n - 1;
            while (j > pivot && nums[j] <= nums[pivot]) {
                j--;
            }
            
            // 4. 交换 nums[pivot] 和 nums[j]
            swap(nums, pivot, j);
            
            // 根据上面的检查也知道,pivot右边一定都是从左往右边看逆序的,逆序一般是最大的情况,
            // 相当于 现在我把 十位搞大了,个位就得干到最小,所以得反转后续
            // 5. 反转 pivot+1 到末尾的部分
            reverse(nums, pivot + 1, n - 1);
        } else {

            // 6. 如果没找到（i=0），说明是完全降序，直接反转整个数组
            reverse(nums, 0, n - 1);
        }
    }
    
    private void swap(int[] nums, int i, int j) {
        int temp = nums[i];
        nums[i] = nums[j];
        nums[j] = temp;
    }
    
    private void reverse(int[] nums, int start, int end) {
        while (start < end) {
            swap(nums, start++, end--);
        }
    }
}
```

## [<font style="color:#DF2A3F;">287. 寻找重复数</font>](https://leetcode.cn/problems/find-the-duplicate-number/)[中等]
给定一个包含 `n + 1` 个整数的数组 `nums` ，其数字都在 `[1, n]` 范围内（包括 `1` 和 `n`），可知至少存在一个重复的整数。

假设 `nums` 只有 **一个重复的整数** ，返回 **这个重复的数** 。

你设计的解决方案必须 **不修改** 数组 `nums` 且只用常量级 `O(1)` 的额外空间。

<!-- 这是一张图片，ocr 内容为： -->
![](https://jiwang.cc.cd/PicGo/lchot100/2026-1774278546-9b69be.png)

**思路：**

这道题的核心在于巧妙地<font style="color:#DF2A3F;">将数组索引与值的映射关系转化为链表结构进行思考</font>：给定一个长度为 n+1 的数组，其中元素取值范围为 1 到 n，

根据抽屉原理必然存在至少一个重复数字。我们可以将每个位置 i 看作链表节点，其值 nums[i] 视为指向下一个节点的指针，

从而将数组抽象为一个逻辑链表。由于值域小于索引范围，必然存在多个索引指向同一位置，这就在链表中形成了一个环。

<font style="color:#DF2A3F;">重复的数字正是环的入口节点——因为它是唯一被多个前驱节点共同指向的位置。</font>

基于这一洞察，我们运用 Floyd 判圈算法：首先使用快慢指针在环中相遇，然后将其中一个指针重置到起点，两个指针以相同速度前进，再次相遇的位置即为环的入口，也就是我们要找的重复数字。

这种方法在 O(n) 时间复杂度和 O(1) 空间复杂度下高效地解决了问题，展现了将数组索引关系转化为链表模型并利用环检测算法的精妙之处。

```java
class Solution {
    public int findDuplicate(int[] nums) {

        // 快慢指针一前一后初始化
        int slow = nums[0];
        int fast = nums[nums[0]];

        while (slow != fast) {
            slow = nums[slow];
            fast = nums[nums[fast]];
        }
        // 这时两指针在环内相遇
        // 设任意一个指针为0,另一个不变,然后以每次只走一步的姿态继续,返回相等的位置,即环的入口值.
        fast = 0;
        while (slow != fast) {
            slow = nums[slow];
            fast = nums[fast];
        }
        return slow;
    }
}
```

